# Muse-Journal (Node.js + Express + EJS)

This is a simple blogging web application built using **Node.js**, **Express.js**, and **EJS**.  
Users can create, view, and manage blog posts. The UI is styled with custom CSS and supports
dynamic routing using Express.

---

## 📌 Features

- Create and view blog posts  
- Dynamic routing (`/view/:id`)  
- EJS templating for server-side rendering  
- Layout components using partials (`header.ejs`, `footer.ejs`)  
- Styled UI with buttons, titles, and responsive layout  
- Separate sidebar showing recent posts  

---

## 🛠 Tech Stack

- **Node.js**
- **Express.js**
- **EJS**
- **CSS / Flex Layout**
- **Bootstrap**
- **JavaScript**

---

## 📁 Folder Structure

